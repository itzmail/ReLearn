type User {
  id: ID!
  name: String!
  fromApi: String
}

type Link {
  id: ID!
  title: String!
  address: String!
  user: User!
}

type Query {
  links: [Link!]!
  users: [User!]!
  link(id: ID!): Link
  user(id: ID!): User
}
